import 'package:flutter/material.dart';

class CashInTab extends StatelessWidget {
  const CashInTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(16),
      child: Text('Cash In'),
    );
  }
}
