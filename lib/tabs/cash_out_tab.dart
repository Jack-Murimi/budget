import 'package:flutter/material.dart';

class CashOutTab extends StatelessWidget {
  const CashOutTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('cash Out');
  }
}
