import 'package:flutter/material.dart';

class CashTransferTab extends StatelessWidget {
  const CashTransferTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Cash transfer');
  }
}
